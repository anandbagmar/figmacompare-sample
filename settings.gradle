rootProject.name = 'figmacompare-sample'

// The reusable Figma <-> Applitools visual-testing framework now lives in its own
// repo/published artifact - see ../figmacompare (io.eot:figmacompare) - and 'samples'
// depends on it as a regular Maven dependency (resolved via mavenLocal). This project
// holds the Bajaj Finserv / Calculator / App Automation Playground / HelloWorld
// scenario providers, the thin TestNG runner shims, and everything that only makes
// sense as an example/consumer of that library.
include 'samples'

