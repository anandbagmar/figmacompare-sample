rootProject.name = 'BajajFinserv-POC'

// 'core' is the reusable Figma <-> Applitools visual-testing framework (no TestNG,
// no this-project-specific test classes) - meant to be extractable into its own
// repo/published artifact later. 'samples' is this project's actual usage of it:
// the Bajaj Finserv / App Automation Playground scenario providers, the thin TestNG
// runner shims, and everything that only makes sense as an example/consumer.
include 'core', 'samples'

